# Hawk React UI

A single page web application for an Eclipse Hawk Docker image, built using React.


## Getting Started
  - Clone the repo and run `npm install` from the root directory to install the necessary packages
  - Run `npm start` and the website will launch in your browser
